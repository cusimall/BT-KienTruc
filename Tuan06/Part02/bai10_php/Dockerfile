# Sử dụng image PHP có Apache
FROM php:8.2-apache

# Copy mã nguồn (trong trường hợp bạn không dùng volume mount)
# COPY src/ /var/www/html/

# (Tùy chọn) Bật mod_rewrite nếu cần cho các framework như Laravel
# RUN a2enmod rewrite